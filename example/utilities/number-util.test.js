const { it, describe } = require('../../bin/oolong');

// Module.
//const { isPrime } = require('./number-util');

describe('isPrime()', () => {
    it('should pass with flying colours', () => {});

    it('should fail miserably', () => {
        let someObject;

        someObject.unknownProperty = 'will break!';
    });
});
